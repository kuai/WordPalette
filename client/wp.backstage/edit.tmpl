<tmpl id="main">
	<div class="edit">
		<form action="post" method="save">
			<div class="sidebar">
				<input type="button" class="sidebar_save" value="`save`">
				<div class="sidebar_item">
					<div class="sidebar_title">`Author and Status`</div>
					TODO<br>
					select author<br>
					select status<br>
					enter shortcut
				</div>
				<div class="sidebar_item">
					<div class="sidebar_title">`Series`</div>
					TODO
				</div>
				<div class="sidebar_item">
					<div class="sidebar_title">`Categories`</div>
					TODO
				</div>
				<div class="sidebar_item">
					<div class="sidebar_title">`Tags`</div>
					TODO
				</div>
			</div>
			<div class="editor">
				<div class="editor_title">
					<input type="text" placeholder="`Post Title`">
				</div>
				<div class="driver"></div>
			</div>
		</form>
	</div>
</tmpl>

<tmpl id="error">
	{{#if system}}`Sorry, an internal server error occurred.`{{/if}}
	{{#if timeout}}`Timed out. Please check your network.`{{/if}}
	{{#if noPermission}}`You are not authorized.`{{/if}}
</tmpl>